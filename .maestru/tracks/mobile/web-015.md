---
maestru: "0.4"
type: work-item
id: WEB-015
title: "Carris: alertas da rede (desvios, obras, interrupções)"
created: 2026-07-15
priority: medium
status: backlog
track: mobile
blocked-by: [WEB-010]
---

# WEB-015: Carris: alertas da rede (desvios, obras, interrupções)

## Contexto

Fase 9 do `.Docs/CarrisDev.txt`: `GET /v2/alerts`, atualização a cada 5 minutos. Funcionalidade não implementada atualmente (não existe consumo de alertas em `useCarris.ts` nem UI para o mostrar).

## Objetivo

- Manter fetch direto a `GET /v2/alerts` a cada 5 min (React Query, sem persistência — dado dinâmico).
- UI nova (não existe hoje) para mostrar alertas ao utilizador — a definir onde encaixa (Transportes.tsx ou secção dedicada).

## Depende de

WEB-010 (modelo de tipos comum). Bloqueia WEB-017.
